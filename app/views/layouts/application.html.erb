<!DOCTYPE html>
<html>
  <head>
    <title>Monde Billing System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
      <div class="container">
        <%= link_to "🏦 Monde Billing", root_path, class: "navbar-brand fw-bold" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to t('navigation.customers'), customers_path,
                          class: "nav-link #{'active' if current_page?(customers_path)}" %>
            </li>
          </ul>

          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "🔧 Jobs", "/sidekiq",
                          class: "nav-link",
                          target: "_blank",
                          title: "Monitoramento de Jobs" %>
            </li>

            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                  👤 <%= current_user.email.split('@').first.capitalize %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li>
                    <h6 class="dropdown-header">
                      <%= current_user.email %>
                    </h6>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to t('navigation.logout'), destroy_user_session_path,
                                class: "dropdown-item text-danger",
                                data: { turbo_method: :delete, turbo_confirm: "Tem certeza?" } %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to t('navigation.login'), new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Cadastrar", new_user_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <div id="flash">
      <% flash.each do |type, message| %>
        <%= render 'shared/flash', type: type, message: message %>
      <% end %>
    </div>

    <!-- Main content -->
    <main class="container py-4">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-light py-3 mt-5">
      <div class="container text-center">
        <small class="text-muted">
          🚀 Powered by Rails 7.1.3 + Hotwire
        </small>
      </div>
    </footer>
  </body>
</html>
