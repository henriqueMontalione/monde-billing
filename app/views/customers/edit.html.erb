<h1><%= t('customers.edit.title') %></h1>

<%= render 'form', customer: @customer %>

<div class="mt-3">
  <%= link_to t('actions.show'), @customer, class: 'btn btn-outline-primary' %>
  <%= link_to t('actions.back'), customers_path, class: 'btn btn-secondary' %>
</div>